import React from 'react';
import { View, SafeAreaView, Text, StyleSheet } from 'react-native';
import ShuttleEvent from '../components/shuttleEvent';

const Home = ({ ShuttleEvent }) => {
  return (
    <View>
      <Text></Text>
    </View>
  );
};

export default Home;
