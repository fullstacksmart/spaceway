import React from 'react';
import {
  FlatList,
  View,
  StyleSheet,
  TouchableOpacity,
  Text,
} from 'react-native';

const MarsWeather = ({ navigation, sols }) => {
  return (
    <View>
      <Text>MARS WEATHER</Text>
    </View>
  );
};

export default MarsWeather;
