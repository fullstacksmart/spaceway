import React from 'react';
import { View, FlatList, SafeAreaView, Text, StyleSheet } from 'react-native';

const SpaceStation = ({ img }) => <View>{img}</View>;

const ISSTracker = ({}) => {};

export default ISSTracker;
