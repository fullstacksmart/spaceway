import React from 'react';
import { View, SafeAreaView, Text, StyleSheet } from 'react-native';

const ShuttleEvent = ({}) => {
  return (
    <View>
      <Text> Discover ISS</Text>
    </View>
  );
};

export default ShuttleEvent;
